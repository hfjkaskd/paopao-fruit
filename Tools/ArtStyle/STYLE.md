# 水果游戏 UI 风格盘点

工作工程为 C:/Projects/paopao/BizzaWZ，按用户要求直接在此工程实施，不创建换皮副本或 QA 工程。用户已回复“可以”，确认暖金边框及蓝色按钮方向；已直接写入当前工程。

执行标准：用户提供的《Unity游戏换皮与视觉验收执行提示词_优化版_v2.md》。该文档要求 PURE_ART、默认不允许 AI 生成、先确认明确效果版本。现有的玩法接入修改不属于本轮视觉变更，不覆盖、不回滚。

唯一风格标准：用户提供的水果树界面。主色为明亮天蓝、叶绿；装饰为暖金；木质使用暖棕色及顺向纹理。高光偏左上，边缘有暖色亮边，阴影柔和。保留有体积感的卡通水果、礼物、纸币和金币。面板采用同体系圆角、木质或蓝金框；不用紫色缎带作为主视觉。动态文字不烘焙进图片，不改金额、文案、奖励数量、按钮、进度和关闭交互。

当前采集：1080×2340 Game View，以编辑器当前缩放截图记录，完整游戏画面在截图中的区域为 (355,120)-(861,1218)。没有将屏幕像素等同于源 PNG 像素。

- 每日广告任务：进入第三关，点击右上礼物，等待异步任务文案和计数出现。
- 提现页：关闭任务弹窗，点击顶部 Retirar，仅浏览页面，未提交提现。
- 提现历史：点击提现页右上历史图标，当前状态为空订单。

`resource-map.json` 包括当前资源哈希、GUID、直接引用与 Prefab 继承依赖。当前共盘点 48 个相关 UI Prefab，121 张公共 UI_Frame 纹理；此数字包含候选资源，不代表它们全部需要替换。仍需核实运行时自定义 Sprite 字段和动态资源加载的全部影响范围。

`Effects/*-comparison.png` 为 R1 配色方向研究：保留当前运行画面的文本、图标、数量、倒计时和布局，只在测量的底板/按钮区域变换颜色。没有 AI 生成。该稿尚未实现木纹材质与逐对象资源映射，**仅作风格方向预览，不能作为完整 1:1 工程替换审批或最终视觉验收证据**。全部页面、长标题、最坏本地化、Sprite alpha 边界及导入参数适配仍待完成。

用户已确认 R1 方向，实施使用原图定向 HSV 调色，未使用 AI，未制作新木纹或更换静态结构。17 张 PNG 的 alpha、尺寸及全部 .meta 字节一致；备份、原始/结果哈希、引用关系见 applied-manifest.json，复跑脚本始终从备份读入。

实际验证正在进行。已从 InitWZ 正式模式启动到第三关，检查每日广告任务、提现、历史，采集 Actual 截图及原图/确认方向/实际运行三图对比。1080×2340 与 1080×1920 两比例均已查看提现及历史。金额、按钮文字和布局保留；倒计时及浮动宝箱随运行时间自然不同。

复测发现 FAQ 固定 800 宽文字内容超过约 749 宽视口。此项单独作为原接入任务要求的文字丢失修复，在 FAQPanel.prefab 将内容水平拉伸、启用布局控制子项宽度，正在复测。没有修改业务代码、FlowModule 或任何 SDK。

已完成修复：FAQ 右侧裁切，重新导入并从 InitWZ 重进后自动换行正常；老虎机/老虎机说明页遗漏 Addressables，已添加 UIPanel/SlotPanel 和 UIPanel/SlotFAQPanel 并实测打开及确认返回。只读诊断记录 FAQ 内容高度 1961.72、视口高度 1035.43，滚轮位置有变化，但当前自动拖动没有滚到底部，完整滚动交互仍未验收。

本轮是已确认配色方向的落地，并非完整主题重绘。老虎机汽车/轮胎装饰仍在；未使用 AI 擅自重绘。已有 17 张纹理共影响 28 个直接引用 Prefab，不能把共享资源覆盖数量等同于实际逐页验证数量。

最后实际检查：设置面板 Configurações 长标题、三个开关图标及继续按钮显示正常，继续返回玩法成功。老虎机最终清理高光杂点版已再次打开截图。日常任务列表为空，定位到 UIDailyTaskPage.OnTabClick 是现有空方法，未伪造任务、奖励或将空列表记作通过。

未完成验收：28 个共享引用 Prefab 的全页面/全状态覆盖、所有广告成功失败/奖励分支、多语言最坏文本、FAQ 拖动到底部、全部水果主题装饰重绘。未做 Android 打包、安装、真机操作、提现或模拟网赚入账。本轮未 commit/push；整合目标未达到完整验收，不能提交为“接入游戏版本”冒充完成。

回滚本轮视觉：按 applied-manifest.json 的 backup 逐项复制 PNG 回 asset；.meta 未改动。FAQPanel.prefab 与 UIPanel.asset 的本轮修改前副本也保存在 OriginalAssets 中。回滚时保留此前玩法接入改动。

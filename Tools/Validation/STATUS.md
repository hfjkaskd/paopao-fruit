# 接入验证状态（2026-09-10）

**当前未通过最终验收。** 正式接口连续返回 `code=1001, message="user is banned"`，`AccountModule` 拒绝加载账号数据，`WKY_SDKTask` 正常中止初始化。需要后台解除测试账号限制，或提供框架现有编辑器配置可用的测试用户 ID，才能继续从 InitWZ 实际复测。没有更换身份绕过限制、关闭正式宏、跳过 SDK 初始化或模拟网赚入账。

第三轮阻断审计：再次在当前 Unity 工程从 InitWZ 实际启动，最新日志仍返回相同 `1001 / user is banned`，玩法 Ready 为 false。网络提示正文与确认按钮可读；提示标题的位置仍有非阻断视觉调整空间。完整编辑器截图保留在私有验证目录，避免把编辑器状态栏中的 SDK 日志随截图提交。任务标记为 blocked，等待有效后台测试条件，未标记完成。

## 工程与 Git

- 唯一接入工程：`C:\Projects\paopao\BizzaWZ`，源自 WKY_FRAME。
- 原游戏只读参考：`C:\Projects\FruitsHarvestMaster\output_Unity`，原仓库保持干净。
- 原游戏画面对照副本：`C:\Projects\FruitsHarvestReferenceValidation`，不作为接入工程。
- origin 已切换至 `https://github.com/hfjkaskd/paopao.git`。
- `框架原始版本` 已提交并推送：`7cc054997c2db92a85256979c85e981e82ece68c`。
- 接入修改保留在当前工作区。尚未以 `接入游戏版本` 提交最终版本，避免把未完成复测的版本标为完成。
- `FlowModule.cs` 未修改；未向两个源仓库提交或推送；未执行 Android 打包、安装或真机操作。

## 接入内容

- 保留 InitWZ、框架加载任务及依赖，通过 BridgingUtil.LoadGamePlayAsync 预加载 HarvestRoot；原加载页视觉配置到 LoadingPanel，由框架控制进度和生命周期。
- 原水果三消玩法资源及 .meta 迁入 FruitsHarvest；路径目录表按类型延迟加载，删除原 SDK、商店、货币、首页及重复结算代码依赖。
- 原玩法嵌入 RealGamePanel，保留框架 GameUiWidget、道具库存栏、设置、成功结算、失败及广告复活流程。
- 关卡开始、胜败、重开、下一关、真实消除剩余组数经适配层连接 FlowModule；存档写入框架 GameSaveData。
- 新人就绪状态每次加载重新计算，避免上次存档让 Teach_01 提前访问空棋盘；UGUI 教学独立输入边界保留真实 TransparentBlock；基础教学完成通知框架。
- 撤销、洗牌、魔法、扩槽使用框架四种道具，均为原实现的立即接受、不可取消模式；原侧不扣库存。框架初始化道具后重新绑定对应库存文本。
- 原教学、满槽提示、道具说明保留。道具教学延后至框架新人流程完成，实际确认才保存已展示状态；撤销动画完成后再次保存快照。
- 后续审查补修：撤销动画开始前即把水果归入棋盘列表，避免动画期间存档漏掉水果或重开遗漏旧对象；恢复同关快照时恢复已用道具/复活次数，避免重进重置限次。这两项尚需实际交互复测。
- 修复失败页缺失图片/本地化键、网络提示页缺失图片/字体，沿用原语言资源；原文本跟随框架语言。移除魔法重复音效调用。
- 原 InputMono 和框架 BizzaButton 使用 Unity 标准 Button 点击派发，保留现有代码回调及按压视觉。

## 实际执行记录

下列“已观察”发生于持续迭代中的版本，不能替代最后修改后的完整回归。

| 项目 | 实际结果与限制 |
| --- | --- |
| Unity 环境 | Unity 2022.3.62f3、Android 编辑器目标，BIZZA_REAL_WITHDRAW 开启；只编译和 Play，不构建 APK。 |
| GUID 检查 | `check_guids.py` 扫描 4754 个 GUID，涉及迁入资源的 GUID 冲突为 0；此检查不证明全部资源引用和运行画面已验收。 |
| InitWZ 启动 | 账号受限前多次实际完成加载并直接进入玩法；原加载视觉已显示。当前最终回归被账号封禁中止。 |
| 第一关操作 | 实际点击九个水果，三组三消，第一关结束、框架关卡增至 2，出现正式“Parabéns”成功页。 |
| 新用户教学 | 已实际操作小手引导及第一关；修复过提前就绪引起的空引用/输入锁。曾实际点击道具说明 Claim 和框架新人奖励入口。后续显示到框架提现教学页，未提交提现交易。最终完整新人链路仍待复测。 |
| 第二阶段教学文本 | 已修复隐藏父对象导致文字和小手一起消失的问题；修复后完整新档复测被封禁阻断。 |
| 重进 | 实际重进至第二关；提前就绪空引用修复后不再在该次重进触发。教学中断及撤销新快照仍需最终复测。 |
| 设置/重开 | 实际打开框架设置，关闭音乐、音效、震动，点击框架重开；第二关保持 2，棋盘重新建立。修改标准按钮派发后尚需完整复测；网络提示确认按钮已实际响应标准 Button。 |
| 失败 | 实际第二关七槽满触发框架失败页；发现白块/缺失文案键，已修改资产与多语言适配，玩法失败页修复后待复测。 |
| 广告复活 | 实际打开框架 MAX 编辑器广告测试，框架成功回调后回收三个槽内水果，七槽减至四槽，复活计数 1。编辑器关闭广告并不等于失败：SDK 自身测试回调可能判成功。 |
| 复活限制 | 复活后再次满槽，实际显示仅重开分支，复活次数仍为 1。 |
| 广告失败/取消 | 未完成。准备使用框架现有 failOpenAd 配置验证失败；不能把关闭测试广告界面当作取消验证。 |
| 四种道具 | 已核对实现、框架库存及结束职责；尚未逐个完成真实点击成功/无目标/库存/次数/中断回归，不报告通过。四种原道具均无目标选择或用户取消阶段。 |
| 原画面对照 | 在独立原游戏参考副本运行第一关、1080×2340，保留截图。接入版曾显示相同水果与背景；尚未完成最后版本同关卡双截图及两种比例的完整画面验收。 |
| 网络提示 | 实际显示后台错误提示，标准确认按钮响应；已修复缺失字体和资源，最后对比度/装饰调整仍需确认。 |

## 恢复测试顺序

1. 提供有效的后台测试条件，通过现有框架编辑器账号配置设置；不要修改 AccountModule、SDK、FlowModule 或新增 Editor 启动绕行。
2. 停止 Play，刷新并编译最后修改，运行资产配置菜单 `Tools/Fruits Harvest/Configure Integration Assets`，从 InitWZ 启动。
3. 本地测试存档备份在 `C:\Projects\paopao-validation-private`（不入 Git）。新档删除的是 GameSaveData/TeachSaveData/ItemSaveData 本地键，不修改账号身份，也不预置教学完成。
4. 新档完整教学及中断重进；确认 Teach_01、基础教学完成、小手/遮罩/输入和新人赠送仅一次；然后逐关自然推进到四种道具解锁，检查库存、每关次数、无目标及重开中断。
5. 复测失败页显示、广告失败/成功/过期回调、限次复活、重开不加关、成功只加一次且下一关实际建立。
6. 在 1080×2340 与 1080×1920 实际检查棋盘、框架入口、道具和弹窗布局，并保存同关卡对照截图。
7. 复测通过后更新本记录，清理编辑器自动生成的无关变化，提交并推送 `接入游戏版本` 至新仓库。

## 未确定项

- 原参考代码的困难关提示方法本身返回 false，关卡配置未提供对应难度判定/百分比规则；没有编造失败率或难度条件。现有真实提示保留，困难规则需要源数据支持后才可实现。
- 目前阻断不属于人工细节调整。账号解除后仍由接入开发继续完成上述修复复测；只有验收通过后的文案措辞、间距及美术微调可作为非阻断人工项。

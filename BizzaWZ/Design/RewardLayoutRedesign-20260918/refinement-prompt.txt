Refine input image 1 (the first generated reward-page preview) with these precise corrections ONLY. Input image 2 is the original screenshot for checking the underlying orchard/tray composition. Keep the entire portrait composition, all prices, two currency rewards, title and level, existing sections, background elements and text unchanged except for the specific corrections below. Output the same portrait image, no border/editor chrome/red markup.

1. IMPORTANT: image1 accidentally lost the original wooden collection tray behind the bottom cash-to-PIX progress row. Restore this dimmed background tray from the original screenshot image2, at exactly its ORIGINAL gameplay location, approximately y=80%..90% of the playable viewport. It must remain BEHIND the modal progress bar and hint. Restore its warm wooden rim, interior, front wall and existing brown right-end locked tile; keep it appropriately dark under the same modal overlay as the tree and lower controls. This is an existing background gameplay object, not a new card for the cash bar. Do not redesign it, enlarge it, shift the bottom tools, or brighten it strongly.

2. In the cream AD BONUS panel, the left/right coin-and-up-arrow icons currently have "+0%" / "+5%" printed across them. This overlap is unacceptable. At EACH end, arrange the SAME existing 3 elements vertically as a neat compact stack: a SMALL coin/up-arrow icon at the top, the percentage on a separate line below, and "Extras" on a separate third line. In the approximately941x1672 image, use icon bounds about46x42 pixels with its bottom at about y=1177, percentage center at y=1194 and "Extras" center at y=1217, with each text line about20 pixels high. Remove ALL percentage digits from the coin/arrow art itself. The left side must show exactly one "+0%" text line and one "Extras"; right exactly one "+5%" and one "Extras". No duplicated percentages. There must be visible gaps between icons and text and between the two text lines. Keep the exact phrase "Assista a vídeos para aumentar as recompensas" centered near the bottom of the same card with clean padding and no overlap with these stacks. Preserve the card position and existing central 6/10 bar, and keep its fill at exactly60%. No extra elements, dividers, captions or decorative leaves.

3. Make the central green "Resgatar×2" button slightly more compact at its SAME CENTER: approximately37% of the viewport width and6% of the height. Keep one blue video/play icon and the exact text "Resgatar×2" in a centered horizontal row with comfortable padding, no leaf or branch decorations. Preserve the ordinary cash-claim line "R$9.6" below it at its current position. Ensure a clear visible gap and no overlapping target regions are implied. Do not add or remove either action. Do not add any text.

4. Refine the bottom cash progress so its fill is precisely25.095% of the inner track (about one quarter); no central number on this bar. Preserve the pink cash icon at left and the existing PIX tile at right, with small gaps between them and the track. Preserve the exact hint "Ganhe mais R$599,24 para sacar R$800" with the two cash amounts in yellow. Keep the bar and hint above the restored darkened tray, not behind it.

Everything else must be preserved from image1, including the unobscured "Nível 2" below "Parabéns", the coins with "1000" and small purple "max", the pink banknotes with "19.2", and the unchanged dimmed top HUD and bottom tools. This is a targeted correction to an existing AI implementation preview. No redesign of the screen structure, no extra controls, no image labels or watermark.

# 主界面重新设计：待确认 AI 效果图

状态：仅预览，等待用户确认。本轮没有修改 Assets、Prefab、Scene 或运行时代码。

## 预览

- `01-user-markup.png`：用户标红的原始截图。
- `02-ai-natural-wood-preview.png`：通过内置 image_gen 编辑生成的 AI 美术效果图，不是 Unity 运行截图或 Prefab 静态预览。
- `prompt.txt`：本次完整生成提示词。

方向：顶部采用独立木牌与叶绿按钮，背后露出天空；设置按钮采用木色与奶油齿轮。每日礼包取消厚圆环，采用奶油纸盒与珊瑚色蝴蝶结。托盘锁按钮采用与托盘协调的木色底和暖铜锁。

## 已核对的工程约束

- 顶部背景属于 RealGamePanel/BG/Image (2)；关卡、两组货币计数、两个动作按钮及设置按钮均已有 Image 可更换。
- 每日礼包已有 240×240 背景与 160×160 礼盒 Image，可用透明留白或柔和投影减轻背景，保留原 Button 范围。
- 托盘锁区背景 162×172，锁图案为独立状态节点。实施时锁不能画死在背景里，需继续按解锁状态显示隐藏。
- 实施仍以现有 Prefab 的节点、坐标、尺寸、Button、动态金额及本地化为准，不根据 AI 图重建或重排结构。
- 未标红的游戏场景、777/进度条和三个底部道具不在此次修改范围。AI 图存在整体重采样，仅用来确认红框区域的美术方向，不作为这些未标红区域的替换素材。

确认后再拆分并适配生产素材；目前没有导入本效果图到游戏工程。

# 提现页对齐和间距修正（2026-09-18）

针对用户提供的三张问题截图，调整既有 `RealWithdrawPanel` 和 `WithdrawLevelItem` 两个 Prefab：

- 等级 Grid 消除向右偏移，与主面板、标题和底条共用中心线。
- 顶部等级、余额、兑换比例对齐。
- 提现按钮与下方说明整体下移 56 UI 单位，提示文案居中；不改变控件顺序和尺寸。
- 倍率标签复用现有 Inset Sprite 和 Body 字体材质，缩至 100×46，采用深青色动态文字，去掉厚橙色胶囊和文字描边。

`02-runtime-pagbank.png` 是 **Unity 实际运行截图**，1080×1920；通过正式 `InitWZ` 启动和原游戏入口打开提现页。不是 AI 效果图或 Prefab 静态预览。对应 JSON 保留原始截图来源和运行时控件数据。

运行核对确认六张等级卡片、动态葡语说明和倍率显示正确，无原截图中的文字/按钮拥挤。运行数据中 13 个原有 Button 均为可交互状态，此字段是组件状态检查，不代表已逐一点击。付款方式切换及实际提现提交未在本轮执行。

`integrity-review.json` 为独立差异审查：365 个序列化对象的层级、组件、ID、顺序和事件绑定保留；1358 个 C# 文件相对既有基线哈希一致。新标签没有新增图像内存或资源加载路径。

`Before/` 为本轮修改前备份；`visual-changes.json` 记录精确字段差异。`apply_polish.py` 仅用于复现这两个 Prefab 的本次变更，后续若编辑过 Prefab，不应直接重跑覆盖。
